package com.kh.collectionEx;
/*
Map
	키(key)-값(value)		한 쌍의 집합을 이루는 구조
	
	키는 숫자, 문자, 문자열 등 어떤 값이 들어가도 괜찮음
	값 또한 모든 값이 들어갈 수 있음
	
	값은 중복이 가능하지만 키는 중복이 될 수 없음
	만약에 중복된 키가 존재한다면 이전 키는 나중에 작성한 키로 대체됨
	순서가 보장되지 않음, 만약 순서를 보장해야하는 경우엔 LinkedHashMap을 사용
	
HashMap		가장 많이 사용
TreeMap		이진 트리로 빠른 검색이 필요할 때 사용
LinkedMap	순서를 보장하는 연결이 필요할 때 사용

메서드
*/
public class Map {

}
