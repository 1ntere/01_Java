package com.kh.VariableEx;
/*
(자료)형변환 : 데이터 타입을 변환하는 것
	자동형변환 / 강제형변환 / 메서드를 이용한 형변환

	1. 자동형변환
		자료형의 byte가 서로 같거나
		변환하는 결과가 변환 전 byte보다 클 경우
			(자료형)을 생략해도 되고 생략하지 않아도 됨
			
			예시)
				int 숫자 = 10;
				double 실수 = (double) 숫자;	//(double) 생략해도 됨
				
	2. 강제형변환
		큰 byte에서 작은 byte로 넘어갈 경우
		큰 그릇에서 작은 그릇으로 이동하기 때문에 강제로 형변환 해줘야 함
			강제로 넣을 경우 데이터 손실 발생 가능성 있음
			(자료형)을 무조건 넣어줘야 함
			
			예시)
				double 실수 = 1.0;
				int 숫자 = (int) 실수;		//(int) 필수로 작성해야 함
				
	3. 메서드를 이용한 형변환
		일부 데이터들은 메서드를 통해 자료형변환이 진행됨
		보통 문자열 String 으로 담은 값을 숫자 계열로 변경하고 싶을 때 많이 사용
		
		예시) 문자열을 정수로 변환하길 원한다면
			String 핸드폰번호 = "01012345678";
			Integer.parseInt(핸드폰번호);	//숫자로 변경
		
		1. <String -> 숫자 자료형>
		바꾸고싶은자료형의객체.parse바꾸고싶은자료형(변환할데이터);
				
		Integer.parseInt(변환할String변수);
		Double.parseDouble(변환할String변수);
		Byte.parseByte(변환할String변수);
		Short.parseShort(변환할String변수);
		Float.parseFloat(변환할String변수);
		Long.parseLong(변환할String변수);
		등등
		
		2. <숫자 자료형 -> String>
		객체자료형.toString(변환할숫자변수);
		
		Integer.toString(변환할숫자변수);
		Double.toString(변환할숫자변수);
		Byte.toString(변환할숫자변수);
		Short.toString(변환할숫자변수);
		Float.toString(변환할실수변수);
		Long.toString(변환할실수변수);
		등등
*/
public class 형변환 {

}
